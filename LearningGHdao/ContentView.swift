//
//  ContentView.swift
//  LearningGHdao
//
//  Created by Anton Tobies on 28.05.23.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("What's up, codecrew!")
            // comment inserted
            // another new comment
        }
        .padding()
    }
}

// another new comment here

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
